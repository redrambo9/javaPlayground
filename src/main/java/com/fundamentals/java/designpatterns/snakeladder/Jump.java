package com.fundamentals.java.designpatterns.snakeladder;

// for snake and ladder you can consider two different objects
// int start, int end but both ladders and snake would have same members start and end
// so let's take jump as a common object which will give the use a jump either up or down

public class Jump {

    int start;
    int end;
}
